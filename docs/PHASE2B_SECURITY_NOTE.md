The selected free staging stack stores runtime provider values only in protected service configuration (Render, GitHub protected migration environment, and provider dashboards). The public repository contains provider names, endpoint constants, templates, and non-secret configuration only. No AppSid, API key, database password, Redis password, R2 secret, customer data, or production secret is committed.